apiVersion: v1
kind: Service
metadata:
  name: agritech-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: agritech-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000