apiVersion: v1
kind: Service
metadata:
  name: agritech-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: agritech-frontend
  ports:
    - protocol: TCP
      port: 8545
      targetPort: 8545